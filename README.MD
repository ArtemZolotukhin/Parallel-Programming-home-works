# Parallel Programming home works

## OpenMP

Programs in openmp_tasks1 are written in C++

### Compilation C++ programms (Linux)

```
g++ -fopenmp program_name.cpp -o program_out_name
```

### Compilation C programms (Linux)

```
gcc -fopenmp program_name.cpp -o program_out_name
```
